public enum TestScenario {
    // Запись объекта TestObject__c обновляет поле TextField__c самой себя.
    SCENARIO_0,

    // После обновления TextField__c у дочернего объекта обновляется поле TextField__c у всех 
    // дочерних объектов того же родителя. Сам родитель не обновляется.
    SCENARIO_1,

    // После обновления TextField__c у дочернего объекта обновляется TextField__c у родителя и
    // затем у всех его дочерних объектов.
    // Либо просто после обновления TextField__c у родителя, оно должно обновиться и у всех дочерних объектов
    SCENARIO_2,

    // Обновление запписи с логирование истории запущенных процессов в поле TextAreaField__c самой записи
    // Количество раз, которое будет меняться поле TextAreaField__c (а следовательно и глубину рекурсии)
    // можно регулировать полем NumberField__c изменяемой записи
    SCENARIO_3
    //Add comment
}